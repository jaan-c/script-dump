import numpy as np
from skimage import data, io
from skimage import transform

# camera = data.camera()
# print(camera.min(), camera.max())

# mask = camera < (255 // 4)
# camera[mask] = 0

# io.imshow(camera)
# io.show()
